<%= form_for(@requirement) do |f| %>
  <% if @requirement.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@requirement.errors.count, "error") %> prohibited this requirement from being saved:</h2>

      <ul>
      <% @requirement.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :req_desc %><br />
    <%= f.text_area :req_desc %>
  </div>
  <div class="field">
  	<%= f.label :specification_id %><br />
	<%= 
	 f.select(:specification_id, Specification.all.collect {|s| [ s.name, s.id ] } )
	 %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
