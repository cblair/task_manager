<%= form_for(@task) do |f| %>
  <% if @task.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@task.errors.count, "error") %> prohibited this task from being saved:</h2>

      <ul>
      <% @task.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :task_desc %><br />
    <%= f.text_field :task_desc %>
  </div>
  <div class="field">
    <%= f.label :est_time %><br />
    <%= f.time_select :est_time %>
  </div>
  <div class="field">
    <%= f.label :act_time %><br />
    <%= f.time_select :act_time %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
